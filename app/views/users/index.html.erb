<br>
<% @users.each do |user| %>
	<%= user.id %>
	<%= user.username %>
	<%= link_to "View", user_path(user), :class=>"btn btn-xs color-success" %>
	<% if user == current_user then %>
		<%= link_to "Edit", edit_user_path(user), :class=>"btn btn-xs color-primary" %>
	<% end %>
	<br>
	<br>
<% end %>