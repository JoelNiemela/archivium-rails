<%= form_for :session, url: login_path do |f| %>
	<%= f.label :email %> <br>
	<%= f.email_field :email, :class=>"input" %> <br>

	<%= f.label :password %> <br>
	<%= f.password_field :password, :class=>"input" %> <br>
	<br>
	<%= f.submit "Login", :class => "btn btn-lg btn-wide btn-primary" %>
<% end%>